version: '3.8'

services:
  nginx:
    build: ./docker/nginx
    container_name: antrian_nginx
    ports:
      - "80:80"
    depends_on:
      - php
    volumes:
      - app_data:/var/www/html
      - ./public/storage/uploads:/var/www/html/public/storage/uploads
    restart: always

  php:
    build: .
    container_name: antrian_app
    depends_on:
      - db
    env_file:
      - .env
    environment:
      - DB_HOST=db
      - WS_PORT=8081
    ports:
      - "8081:8081"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - app_data:/var/www/html
      - ./public/storage/uploads:/var/www/html/public/storage/uploads
    restart: always

  db:
    image: mysql:8.0
    container_name: antrian_db
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASS:-root}
      MYSQL_DATABASE: ${DB_NAME:-aplikasi_antrian}
    volumes:
      - ./database/aplikasi_antrian.sql:/docker-entrypoint-initdb.d/init.sql
      - db_data:/var/lib/mysql
    ports:
      - "3307:3306"
    restart: always

volumes:
  db_data:
  app_data:
